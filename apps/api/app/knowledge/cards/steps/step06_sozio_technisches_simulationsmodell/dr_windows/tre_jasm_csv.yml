id: "artifact:steps/step06_sozio_technisches_simulationsmodell/dr_windows/tre_jasm_csv"
doc_type: "data_artifact"
title: "Artefakt: tre02-Export tre_jasm_{appliance_slug}_{year}_top{N}[_fx{FX}].csv"
lang: "de"
location_pattern: "data/market/processed/dr_windows/tre_jasm_{appliance_slug}_{year}_top{N}[_fx{FX}].csv"
tags: ["jasm","export","dr_windows","timeseries"]
summary: >
  CSV mit JASM-Lastprofilen für ein Zielgerät, gefiltert auf die Top-TRE-Tage
  (ermittelt in tre02). Dient als Zwischenprodukt für nachfolgende Schritte.
schema:
  index: "timestamp (ISO, von load_appliances; keine TZ-Anreicherung durch tre02)"
  columns:
    - "<appliance>: float (aggregierte Leistung/Energie je Zeitschritt, wie vom Loader geliefert)"
notes:
  - "Auflösung hängt vom Loader ab (meist stündlich)."
  - "Dateiname enthält Gerät (slug), Jahr, Top-N und optional FX."
embed_fields: ["title","summary","schema","notes"]
